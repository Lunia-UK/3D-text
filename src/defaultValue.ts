export const defaultTextValue = {
  text: "Hello there !",
};

export const defaultColor = 0xc9c9c9;

export const sizes = {
  width: window.innerWidth,
  height: window.innerHeight,
};

export const defaultTextProps = {
  size: 0.5,
  height: 0.2,
  curveSegments: 12,
  bevelEnabled: true,
  bevelThickness: 0.03,
  bevelSize: 0.02,
  bevelOffset: 0,
  bevelSegments: 5,
};

export const defaultFontFamily = "helvetiker";

export const fonts = ["helvetiker", "gentilis", "optimer"];

export const defaultFont = {
  fontFamily: defaultFontFamily,
  value: defaultTextValue.text,
};
